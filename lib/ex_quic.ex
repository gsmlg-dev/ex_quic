defmodule ExQuic do
  @moduledoc """
  Experimental QUIC v1 building blocks.

  The first release slice provides pure wire/protection helpers and runtime
  ownership contracts. It does not expose a network connect or send success.
  """
end
